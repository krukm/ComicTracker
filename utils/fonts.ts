import { Patrick_Hand_SC, Roboto_Mono, Rubik_Glitch } from 'next/font/google'

export const robotoMono = Roboto_Mono({
  subsets: ['latin'],
  weight: '400',
})

export const rubikGlitch = Rubik_Glitch({
  subsets: ['latin'],
  weight: '400',
})

export const scriptFont = Patrick_Hand_SC({
  subsets: ['latin'],
  weight: '400',
})
